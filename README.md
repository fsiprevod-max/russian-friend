# Russian Friend

Upload all files in this folder to the GitHub repository and enable GitHub Pages. The entry point is `index.html`. Serbian audio is split across `audio01.js` through `audio05.js`, each comfortably below GitHub's 25 MiB browser upload limit.
